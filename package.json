{
  "name": "the-school-management",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install",
    "dev": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
